= link_to 'New', new_user_path
= render 'search_form'
%table
  %thead
    %tr
      %th id
      %th Name
      %th Phone Numbers
      %th Address Line
      %th Address Zip
      %th Actions
  %tbody
    = render @users